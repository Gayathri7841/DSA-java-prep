class Solution {
    public int elevatorRequests(int n, int[] requests) {
        int sum=Math.abs(0-requests[0]);
        for(int i=1;i<requests.length;i++){
          sum=sum+  Math.abs(requests[i]-requests[i-1]);
        }
        return sum;
    }
}
