class Solution {
    public int countRatioSubarrays(int[] nums, int a, int b) {
    int n=nums.length;
        int count=0;
        for(int i=0;i<n;i++){
            int oddcount=0;
            int evencount=0;
           
            for(int j=i;j<n;j++){
               if(nums[j]%2==0) evencount++;
                else oddcount++;
                 if(oddcount==0) continue;
            if(1L*evencount*b<=1L*oddcount*a) count++;
          
            }
           
        }
        return count;
    }
}
